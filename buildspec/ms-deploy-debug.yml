version: 0.2

phases:
  pre_build: 
    commands:
      - rm -rf package-lock.json
      - npm i
      - cd libs
      - npm i
      - cd .. 

  build:
    commands:
      - ./deployDiff.sh dev us-east-1
