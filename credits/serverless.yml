service: credits

provider:
  name: aws
  runtime: nodejs6.10
  stage: dev
  memorySize: 128
  timeout: 60
  environment:
    COLL_NAME: credits
    DB_NAME: crowdaaDev
    MONGO_URL: mongodb://crowdaaDev:crowdaaDev@aws-eu-west-1-portal.10.dblayer.com:18012,aws-eu-west-1-portal.8.dblayer.com:18012/crowdaaDev?ssl=true

functions:
  getCredits:
    handler: handler.handleGetCredits
    events:
      - http:
          path: /
          method: get
          authorizer: arn:aws:lambda:us-east-1:630176884077:function:account-dev-authorize
          cors: true

  addCredits:
    handler: handler.handleAddCredits

plugins:
  - serverless-domain-manager
  - serverless-webpack

package:
  individually: true

custom:
  customDomain:
    domainName: 'api.aws.crowdaa.com'
    basePath: 'credits'
    stage: ${self:provider.stage}
    createRoute53Record: true
  webpackIncludeModules: true
