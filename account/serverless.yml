service: account

provider:
  name: aws
  runtime: nodejs6.10
  memorySize: 128
  timeout: 60
  environment:
    MONGO_URL: mongodb://crowdaaDev:crowdaaDev@aws-eu-west-1-portal.10.dblayer.com:18012,aws-eu-west-1-portal.8.dblayer.com:18012/crowdaaDev?ssl=true

functions:
  authorize:
    handler: handler.handleAuthorize
    
plugins:
  - serverless-webpack

package:
  individually: true

custom:
  webpackIncludeModules: true
