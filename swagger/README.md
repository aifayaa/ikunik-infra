# Swagger documentation for Crowdaa Microservices

## Installing

```sh
npm install
```

If running on root, you may need to also run `npm run install` after it.

## Building

```sh
npm run build
```

The build output will be placed in a new `dist` folder.

## Starting locally and debugging

```sh
npm run start
```
